services:
  mongo:
    image: mongo:7
    container_name: whatsbot-mongo
    restart: unless-stopped
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_DATABASE: whatsbot
    volumes:
      - whatsbot-mongo-data:/data/db

volumes:
  whatsbot-mongo-data:
